import 'package:flutter/material.dart';

class JoinChamberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Join Chamber'),
      ),
      body: Center(
        child: Text('Join Chamber Page Content'),
      ),
    );
  }
}
