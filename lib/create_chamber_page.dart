import 'package:flutter/material.dart';

class CreateChamberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Chamber'),
      ),
      body: Center(
        child: Text('Create Chamber Page Content'),
      ),
    );
  }
}
